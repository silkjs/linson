import { Locale } from "../locales-type";

export const enUS: Locale = {
  pagination: {
    items_per_page: "{size} / page",
    jump_to: "jump to",
    page: "",
    total: "{total} items",
  },
};
