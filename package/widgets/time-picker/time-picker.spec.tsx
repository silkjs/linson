import { mount } from "@vue/test-utils";
import { TimePicker } from "./time-picker";

describe("时间选择器 TimePicker", () => {
  it("props default", () => {
    const wrapper = mount(TimePicker, {});
  });
});
