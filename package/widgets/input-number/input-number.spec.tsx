import { mount } from "@vue/test-utils";
import { InputNumber } from "./input-number";

describe("数字输入框 InputNumber", () => {
  it("props default", () => {
    const wrapper = mount(InputNumber, {});
  });
});
