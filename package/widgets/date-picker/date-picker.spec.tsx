import { mount } from "@vue/test-utils";
import { DatePicker } from "./date-picker";

describe("日期选择器 DatePicker", () => {
  it("props default", () => {
    const wrapper = mount(DatePicker, {});
  });
});
