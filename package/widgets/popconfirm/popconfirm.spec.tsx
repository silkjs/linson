import { mount } from "@vue/test-utils";
import { Popconfirm } from "./popconfirm";

describe("气泡确认框 Popconfirm", () => {
  it("props default", () => {
    const wrapper = mount(Popconfirm, {});
  });
});
