import { mount } from "@vue/test-utils";
import { CheckboxGroup } from "./checkbox-group";

describe("复选框 CheckboxGroup", () => {
  it("props default", () => {
    const wrapper = mount(CheckboxGroup, {});
  });
});
