import { mount } from "@vue/test-utils";
import { InputGroup } from "./input-group";

describe("输入框 InputGroup", () => {
  it("props default", () => {
    const wrapper = mount(InputGroup, {});
  });
});
