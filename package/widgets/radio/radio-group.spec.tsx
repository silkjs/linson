import { mount } from "@vue/test-utils";
import { RadioGroup } from "./radio-group";

describe("单选框 RadioGroup", () => {
  it("props default", () => {
    const wrapper = mount(RadioGroup, {});
  });
});
